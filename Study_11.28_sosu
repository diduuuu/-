#include <stdio.h>

int main(void) {
int a, b, count=0;

scanf("%d %d",&a,&b);

for (int i = a; i <= b; i++){
  for (int j = 1; j <= i; j++) if (i%j==0) count++;
  if(count==2) printf("%d\n",i);
  count = 0;
}

  return 0;
}
